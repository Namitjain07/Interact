<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@android:color/transparent"
    android:layout_gravity="center_vertical"
    android:minHeight="?attr/actionBarSize">

    <!-- App Name -->
    <TextView
        android:id="@+id/tvAppName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Interact"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textFontWeight="600"
        android:textColor="@android:color/white"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="16dp"/>

    <!-- Camera and Audio Controls -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="end|center_vertical">

        <ImageButton
            android:id="@+id/btnAudioOptions"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@android:color/transparent"
            android:contentDescription="Choose audio options"
            android:src="@drawable/baseline_volume_up_24"
            android:padding="12dp" />

        <ImageButton
            android:id="@+id/btnSwitchCamera"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@android:color/transparent"
            android:contentDescription="Choose camera options"
            android:src="@drawable/baseline_flip_camera_android_24"
            android:padding="12dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="16dp"/>
    </LinearLayout>
</LinearLayout>
